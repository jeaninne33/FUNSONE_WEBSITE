<div class="page_info">
    <h3 class="aligncenter"> <span> <i class="fa fa-user"></i></span>
    Inicio de Sesion </h3>
  </div>
      <div class="row">
        <div class="col-sm-6 col-sm-offset-3 form-box">
          <div class="form-top">
            <div class="form-top-left">

              <h5>Accede a nuestro sitio</h5>

          <h6>  <i>Introduzca su nombre de usuario y contraseña para iniciar sesión:</i></h6>
            </div>
              <div class="dt-sc-margin20"></div>
            <div class="form-top-right"></div>
          </div>

          <div class="form-bottom">
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <div class="field">
              <i class="fa fa-envelope">
                Email</i>
              <%= f.email_field :email, autofocus: true, class:"form-username form-control" %>
            </div>

            <div class="field">
              <i class="fa fa-lock">
                Contrasena</i>
              <%= f.password_field :password, autocomplete: "off", class:"form-username form-control" %>
            </div>

            <% if devise_mapping.rememberable? -%>
            <div class="field">
              <%= f.check_box :remember_me %>
              <b> Recordarme?</b>
            </div>
            <% end -%>

            <div class="actions">
                     <button type="submit" class="dt-sc-button1 ico-button" style=" font-size: 15px; padding: 15px 29px; ">
    <i class="fa fa-user"></i>
    Ingresar
  </button>

            </div>
            <% end %>
          </div>
        </div>
      </div>
  <%= render "devise/shared/links" %>
